<%= simple_form_for [@shop, @product] do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :info %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.button :submit %>
<% end %>
